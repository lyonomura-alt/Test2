/* global Editor */
window.Renderer=(()=>{const dash=[4,40];function boundary(ctx,l,points){const b=l.boundary;if(!b||b.points.length<2)return;ctx.save();ctx.strokeStyle='#ffe600';ctx.lineWidth=3/l.scale;ctx.lineCap='butt';ctx.setLineDash(dash.map(n=>n/l.scale));ctx.beginPath();ctx.moveTo(b.points[0].x,b.points[0].y);for(let i=1;i<b.points.length;i++){const p=b.points[i];if(b.segments[i-1]==='curve'&&i<b.points.length-1){const n=b.points[i+1];ctx.quadraticCurveTo(p.x,p.y,(p.x+n.x)/2,(p.y+n.y)/2)}else ctx.lineTo(p.x,p.y)}ctx.stroke();ctx.setLineDash([]);if(points&&Editor.mode==='boundary'){ctx.fillStyle='#ffe600';b.points.forEach(p=>{ctx.beginPath();ctx.arc(p.x,p.y,7/l.scale,0,Math.PI*2);ctx.fill()})}ctx.restore()}
function cutPreview(ctx,l){const p=Editor.cutPreview;if(Editor.mode!=='cut'||!p||p.length<2)return;ctx.save();ctx.strokeStyle='#fff';ctx.lineWidth=2.5/l.scale;ctx.lineCap='round';ctx.lineJoin='round';ctx.setLineDash([8/l.scale,6/l.scale]);ctx.beginPath();ctx.moveTo(p[0].x,p[0].y);p.slice(1).forEach(q=>ctx.lineTo(q.x,q.y));ctx.stroke();ctx.restore()}
function layer(ctx,l,selected){if(!l.image)return;const w=l.image.naturalWidth,h=l.image.naturalHeight;ctx.save();ctx.translate(l.x,l.y);ctx.rotate(l.rotation);ctx.scale(l.scale,l.scale);ctx.globalAlpha=l.opacity;if(l.mask){const temp=document.createElement('canvas');temp.width=w;temp.height=h;const t=temp.getContext('2d');t.drawImage(l.image,0,0);t.globalCompositeOperation='destination-out';t.drawImage(l.mask,0,0);ctx.drawImage(temp,-w/2,-h/2)}else ctx.drawImage(l.image,-w/2,-h/2);ctx.globalAlpha=1;boundary(ctx,l,selected);if(selected)cutPreview(ctx,l);ctx.restore()}
function render(){if(!window.Editor?.canvas)return;const c=Editor.canvas,ctx=c.getContext('2d'),d=devicePixelRatio||1;ctx.setTransform(d,0,0,d,0,0);ctx.clearRect(0,0,c.width/d,c.height/d);Editor.layers.forEach((l,i)=>layer(ctx,l,i===Editor.selected))}
function exportPNG(){const source=Editor.canvas,out=document.createElement('canvas');out.width=source.width;out.height=source.height;const ctx=out.getContext('2d');ctx.fillStyle='#fff';ctx.fillRect(0,0,out.width,out.height);ctx.drawImage(source,0,0);const a=document.createElement('a');a.download='two-image-composite.png';a.href=out.toDataURL('image/png');a.click()}return{render,exportPNG}})();